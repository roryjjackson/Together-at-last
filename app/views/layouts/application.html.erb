<!DOCTYPE html>
<html>
  <head>
    <title>Togetheratlast</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel&family=Noto+Sans:wght@300&family=Rammetto+One&family=Roboto:wght@300;500&display=swap" rel="stylesheet">
  </head>

  <body>
    <%# <%= render "shared/navbar" %>
    <%= render 'shared/navbar' unless current_page?(root_path)%>
    <%= render "shared/flashes" %>

    <%= yield %>
  </body>
</html>
