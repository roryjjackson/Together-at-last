<%= simple_form_for([@wedding, @vip]) do |f| %>
<%= f.input :name %>
<%= f.input :description %>
<%= f.input :category %>
<%= f.input :photos, as: :file, input_html: { multiple: true } %>
<%= f.submit "add vip", class: "btn btn-primary" %>
<% end %>
